# Admin Authentication (Choose ONE method)
# Option 1: Plain text password (less secure, for development)
ADMIN_PASSWORD=your-secure-password-here

# Option 2: Hashed password (more secure, for production)
# Generate with: node scripts/hash-password.js your-password
ADMIN_PASSWORD_HASH=$2a$12$your-bcrypt-hash-here

# JWT Secret (required for session management)
JWT_SECRET=your-very-long-random-jwt-secret-key-here

# Next.js Configuration
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Security Notes:
# - Use ADMIN_PASSWORD_HASH in production instead of ADMIN_PASSWORD
# - JWT_SECRET should be a long, random string (minimum 32 characters)
# - Never commit actual secrets to version control
